[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "dss"
version = "0.1.0"
requires-python = ">=3.9"
license = "MIT"

[tool.maturin]
module-name = "dss"
features = ["python"]
